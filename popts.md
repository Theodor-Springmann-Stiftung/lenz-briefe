PRE:
Ok, here is a new project.
It is the edition of the letters of an 18th and early 19th century author, Jacob Lenz in xml.
To gain some context read the xsd schema in data/xsd/, some (parts of) example files, at least in data/xml/briefe.xml and traditions.xm,l as well as references.xml and meat.xml.
Gain some context. We will (not yet) make a website to display the letters. For now: Make a list of all tags/elements + attribute combinations that can occur in briefe.xml.
The next step would be to tell you how each tag should be formatted, and how a minimum viable website should look. But that is the next step after this task.




I:
Ok now; the next step is to analyze the xsd transformations in transform/ and see if they are met by our website requirements or what and in whiuch form we will need the html fragments generated by it. The final website will be build with astro, i'd just like to do transforms in python (ignore js transforms) that give us every essential usable parts of the website (such as structured meta data, text etc).
But we nont need to build the astro site right now. I will give you the specs and let's see if we output the right fragments to make it easy for us to put it together in astro. Then we will rewrite the xsd to do nothing more, jsut allow us, to do the minimum viable implementation later in astro. Let's cut all crap that is too much and concentrate on the details.
The result should be a STATIC site build with astro; later int he process.
First, the home page and letter meta data: we ill probably want to output some structured json. All filtering must be done client-side since we want a static page
The home page needs to:
- List all letters by date by default (when or if NA from or if NA notBefore or if NA to or if NA notAfter).
- Have some sort of paginations (custom year groups 1756–1770, 1771–1775, 1776, 1777–1779, 1780–1792), so some form of pagination
- allow filtering by persons
- allow filtering by places
So we need to output specialized json for that. Also each header in the list must show:
- human readable sent date (from sent, literally in the XML) an recive date in brackets []
- wheather the original was available or not, and if not wheather the source is a manuscript or print
- the sender(s) and sending places in paranthesis (place)
- the receiver(s) and receiving places CAREFUL: some letters have multiple sent and receive events!)
- wheather or not we have an entfurf or an actually sent letter
this header must be in the list (eventually filtered) of the index; but also above each letter text on the single letter page.
the single letter page is a bit more complicated. We have a mix of block and non-block-elements. Each letterText represents the text of a letter. The difficulty lies in block and non-block elements: block-level elements in the html may correspond to non-blocks in the xml and vice versa.
The letter text will autoflow in a middle column (about 75ch wide, no hyphens) of the page. The main structure of each letter is: page (which may or may not be a block), semantic line-breaks (line) of different types, semantic verrtical space (vspace) and sidenotes (which belong into a seperate container as the letter, and are assigned to a page).
I really dont like to model pages as blocks, but rather line and vspace contents as blocks. Because page breaks are either in the middle of a block (if the author jsut used a new page by space requirements). Or, sometimes pages start with a semantically relavant new line (ALL semantically relavant newlines (ex in sidenotes) are specified in the xml). This difference is why it's hard to model pages as blocks, because they sometimes are just pagebreaks inside a block that is a paragraph.
in the xml some formatting can span lines: hb, ru, gr, fr (all the languages, only important for correct hyphenation if ever hyphenated, but for now irrelevant), aq, it, large, ul, dul, tul, highlight, del, er, insertion, subst, undo, hand, ink, pe, note, tl, nr, anchor, fn
Block levels are, as you can see, cant contain lines: align, tab. These are inline blocks: align can be inside a line left aligned AND center aligned. Or, if text has no align in a line, left aligned and additionally right aligned in the same line if there is text present in a line with and without align tags. (also in any combination with center and right). tab is something similar, "blocks inside a line" but more controllable by a fine-grain
Sidenotes a re a different story: they belong to a page (but will be displayed at the height of the page break on desktops in a left margin). They also can contain semantic line breaks; that are relavant in the context of a line. 
Any vspace breaks the line context. itr means the last line ahs ended an now we have emtpty vertical space. There is a semantically relavant line break after every vspace.
Lines have the tab  value to both control the line indent and also somewhat encode the line meaning eg. in  a poem all the verses have the same indent). all lines are line type break (so break is irrelevant). line type line is jsut a line with a horizontal line 
We can ignore the tags address for now (not the contents, but the tag).
The traditions are printed below the letters and can contain additionally similar styled text as the lettewr text in each <app>
Look at our xslt; make a model how you would model the documents. Look if it fits the model, present the model and what you would change and how it would work .





Show less



II:
Ok we'll use astro; but only for generating a strictly static page. We use tailwind to the most extend as possible. For fonts etc. we use tailwind settings. For the styles for the text we can use CSS classes with the @apply syntax to apply tailwind styling as much as possible
The index page displays letter meta data. Query stings denote active pages and filters. Everything is done in js for the navigation; between pages and filters.

The languages: gr, fr, hb, ru have no formatting consequences. We just set the lang attribute as well as the lang attribute means lang latin (la). Aq has a formatting consequence, but also the lang attribute (see below)

Some formtting only makes sense if the source of the letter is a printed tradition: such as bold and italic I guess. Ink and Pencil only if the basis is a manuscript. Can you eruate that? Also for del etc?



III:
Ok, make a list of all the tags wheather they make sense in print or manusicript or both. then look at the letters if they match. Make a list of exceptions that dont match and I will go take a look at the places. Either the tags mean sth different that I am thinking or maybe the metadata is not correct or we have cases that are kinda difficult and edge cases. I have to look at this manually but I need a list


